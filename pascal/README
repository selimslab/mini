## inter-pretty

a simple PASCAL interpreter in Python

inspired by Peter Norvig's Lisp interpreter and Ruslan Spivak's Pascal interpreter

input => Tokenizer(lexer, lexical analysis) => tokens => Parser (syntax analysis) => abstract syntax tree => SemanticAnalyzer => Interpreter => output

`python repl.py` 
